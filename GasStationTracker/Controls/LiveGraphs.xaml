<UserControl x:Class="GasStationTracker.Controls.LiveGraphs"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GasStationTracker.Controls" xmlns:oxy="http://oxyplot.org/wpf" xmlns:gasstationtracker="clr-namespace:GasStationTracker"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.DataContext>
            <gasstationtracker:GraphViewModel/>
        </Grid.DataContext>
        <Grid Background="LightGray" Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"></ColumnDefinition>
                <ColumnDefinition Width="*"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"></RowDefinition>
                <RowDefinition Height="*"></RowDefinition>
            </Grid.RowDefinitions>
            <Expander x:Name="expander" Grid.ColumnSpan="2" ExpandDirection="Down" IsExpanded="False" HorizontalAlignment="Stretch">
                <Expander.Header>
                    <TextBlock TextAlignment="Left" HorizontalAlignment="Center" VerticalAlignment="Center" Text="View" FontWeight="Bold"/>
                </Expander.Header>
                <Popup Grid.ColumnSpan="2" PlacementTarget="{Binding ElementName=expander}"
                   Width="{Binding ActualWidth, ElementName=expander}"
                   Placement="Bottom"
                   IsOpen="{Binding IsExpanded, ElementName=expander}">
                    <Grid x:Name="ViewSettings" Grid.Row="1" Grid.ColumnSpan="2">
                        <Grid.Background>
                            <SolidColorBrush Color="#f5f5f5"/>
                        </Grid.Background>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"></ColumnDefinition>
                            <ColumnDefinition Width="*"></ColumnDefinition>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"></RowDefinition>
                            <RowDefinition Height="*"></RowDefinition>
                            <RowDefinition Height="*"></RowDefinition>
                            <RowDefinition Height="*"></RowDefinition>
                            <RowDefinition Height="*"></RowDefinition>
                            <RowDefinition Height="*"></RowDefinition>
                        </Grid.RowDefinitions>
                        <TextBlock Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" FontWeight="Bold">Context:</TextBlock>
                        <CheckBox Grid.Row="1" Grid.Column="0">Cash</CheckBox>
                        <CheckBox Grid.Row="2" Grid.Column="0">Popularity</CheckBox>
                        <CheckBox Grid.Row="3" Grid.Column="0">Money earned on fuel</CheckBox>
                        <CheckBox Grid.Row="4" Grid.Column="0">Money spent on fuel</CheckBox>
                        <CheckBox Grid.Row="5" Grid.Column="0">Current Fuel</CheckBox>
                    </Grid>
                </Popup>
            </Expander>
        </Grid>
        <oxy:PlotView x:Name="Graph" Background="Transparent" Grid.Row="1" Model="{Binding Plot}"></oxy:PlotView>
    </Grid>
</UserControl>
